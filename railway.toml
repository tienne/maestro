[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @maestro/relay-server build"

[deploy]
startCommand = "node packages/relay-server/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "maestro-relay"
